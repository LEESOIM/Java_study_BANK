<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.iu.start.bankbook.BankBookCommentDAO">

	<insert id="setCommentAdd" parameterType="BankBookCommentDTO">
		INSERT INTO BB_COMMENT VALUES(BOARD_SEQ.NEXTVAL,#{bookNum},#{writer},#{contents},SYSDATE)
	</insert>
	
	<select id="getCommentList" parameterType="CommentPager" resultType="BankBookCommentDTO">
		SELECT * FROM
	    (SELECT ROWNUM R, C.* FROM
		(SELECT * FROM BB_COMMENT WHERE BOOKNUM=#{bookNum} ORDER BY NUM DESC) C) <!-- 해당글의 댓글만 불러와서 C 테이블 정렬 먼저 -->
		WHERE R BETWEEN #{startRow} AND #{lastRow}
	</select>
	
	<select id="getCommentListTotalCount" parameterType="CommentPager" resultType="java.lang.Long">
		SELECT COUNT(NUM) FROM BB_COMMENT WHERE BOOKNUM=#{bookNum}
	</select>

	<delete id="setCommentDelete" parameterType="BankBookCommentDTO">
		DELETE BB_COMMENT WHERE NUM=#{num}
	</delete>
	
	
</mapper>